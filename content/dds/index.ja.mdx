---
title: Digimaのデザインシステムを立ち上げ、リードし続ける ― 開発スピードの向上、一貫性の強化、そしてエージェント型デザインの基盤づくり。
tags: [DesignSystem, DesignEngineering, AgenticDesign, FrontEndDevelopment]
path: articles/design-system
order: 6
featured: 0
displayToc: true
coverHomepage: images/dds-homepage-cover@2x.png
coverArticle: images/article-cover.png
menuVignettes: images/menu-vignettes.png
mobileVignettes: images/mobile-menu-vignettes.png
suggestedArt: images/suggestedArt2.png
imagesMd: 
    - images/inventory-spreadsheet-jp.png
    - images/v1-long-list.png
    - images/visual-direction-01.png
    - images/visual-direction-02.png
    - images/visual-direction-final-01.png
    - images/master-file-1.png
    - images/master-file-2.png
    - images/template-rules.png
    - images/Components-1.png
    - images/documentation-site.png
    - images/invision-inspect.png
    - images/live-compoenents-jp.png
    - images/before-profile-before.png
    - images/before-profile-after-jp.png
    - images/before-table-before.png
    - images/before-table-after-jp.png
    - images/before-dashbaord-before.png
    - images/before-dashbaord-after-jp.png

imagesXl: 
    - images/v1-duplicate-styles-ja.png
    - images/Components.png
    - images/token_architecture_diagram-jp.png
    - images/figure-1-architecture-ja.png
    - images/figure-3-code-to-figma-ja.png
    - images/figure-2-figma-to-code-ja.png
---

<!-- Overview -->

<ArticleOverview>
    <Sunnary>
        Digima は、日本の中小規模の不動産会社向け B2B SaaS CRM です。初期バージョンは、デザインとコードの両方を一人のエンジニアが担当して開発されたもので、明確なプロセスもデザインシステムも存在しませんでした。その結果、私が Comvex に入社した頃には、プロダクトは深刻な一貫性の欠如に陥っており、新機能を追加するたびに開発が難しくなっていました。チームはプロダクトを一から作り直すことを決め、私はそのデザイン領域全体を担当することになりました。
    </Sunnary>
    <Problems>
        <ul>
            <li>より拡張性のある方法で、機能のデザインを継続的に提供できるようにする必要がありました。</li>
            <li>Digima のユーザーは、より一貫性のあるユーザー体験を必要としていました。</li>
            <li>デザインとエンジニアリングの間に共通の用語体系がなく、機能のハンドオフのたびに本来不要な時間がかかっていました。</li>
        </ul>        
    </Problems>
    <Goals>
        <ul>
            <li>デザインプロセスに品質・一貫性・スピードをもたらすツールとプロセスを構築する。</li>
            <li>それらのツールを使ってプロダクトをリデザインする。</li>
            <li>デザインの意図と、実際に出荷されるコードとの距離を縮める。</li>
        </ul>      
    </Goals>
    <Outcomes>
        <ul>
            <li>ゼロから構築したデザインシステムが、現在も継続的に運用されている</li>
            <li>デザイントークンをフロントエンドのコードベースに実装し、自作のLintルールで強制。ハードコードされた値はマージ時に弾かれ、ビジュアルの不一致を確認するQAという作業自体が不要になった</li>
            <li>アプリケーションのコードベースに直接統合されたStorybookのドキュメント層を構築。現在約10名のエンジニアが日常的に活用</li>
            <li>Figma とコードベースの一致を保つトークン同期システムを構築。初回の全体比較では、既存のどのチェックにも検出できなかった 4 件の不整合が見つかり、現在は両者のズレはゼロ</li>
            <li>次のステップへの基盤も完成：エージェント型デザインシステムへ</li>
        </ul>        
    </Outcomes>
    <Role>
        <ul>
            <li>デザインシステムをゼロから立ち上げ、その後の発展もすべてリードしてきました。</li>
            <li>主要なアーキテクチャの転換をすべて主導しました。Sketch から Figma への移行、トークンと Lint ルールによるコードベースへの統合、Figma とコード間のトークン同期、そして現在進行中の AI による生成支援への展開などです。</li>
            <li>システムの構造を設計し、そのルールを定め、変更内容を承認する責任を担っています。</li>
            <li>エンジニアによるコンポーネントの追加やリファクタリングをレビューし、QA を行っています。</li>
        </ul>   
    </Role>  
</ArticleOverview>

import ImageSlider from "../../src/components/image-slider"

<!-- Content -->
<div className="articleContent">
<section id="audit">

#### 監査
## プロダクトの監査によって、多数の一貫性の欠如と UX 上の課題が明らかになり、デザインシステムが必要であることが確認できました。

<p>
何かを提案する前に、私はまず既存のプロダクトを徹底的に評価することから始めました。プロダクトの UI を一つひとつ確認し、すべてのメニュー、ボタン、モーダル、フォームをキャプチャしました。それらをスプレッドシートに整理し、種類ごとにグループ化して、各バリエーションに名前を付けました。その結果、この後のすべての判断を裏付ける数字が浮かび上がりました。
</p>

<ul>
    <li>プロダクト全体で 130 種類の異なる色が使用されていた</li>
    <li>56 種類のグレーが混在していた</li>
    <li>22 種類の異なるフォントサイズが存在した</li>
    <li>検索フィールドには、まったく異なる 4 種類のバリエーションが存在した</li>
</ul>

<Images name={props.images.md["inventory-spreadsheet-jp"]}/>

<p>
すべての要素を並べて見たことで、Digima のニーズに合わせて設計された、整理されたコラボレーティブなデザインシステムが必要であることが明確になりました。
</p>

<Images 
    containerType="full" 
    className="bg-gray-0" 
    containerWidth="Xl"
    name={props.images.xl["v1-duplicate-styles-ja"]}
    captions="当時のプロダクトの監査によって、多くの一貫性の欠如が明らかになりました。"
/>
</section>
<section id="principles-and-visual-direction">

#### 原則とビジュアル
## コンポーネントの設計に入る前に、チームと私は 4 つの原則について合意しました。これによって、その後の判断のたびに同じ議論を繰り返さなくても、一貫した意思決定ができるようになりました。

<p>
チームでの議論から生まれた 4 つの原則は、今に至るまで一貫して機能し続けています。
</p>

<ul>
    <li><strong>目的を持って取り組む:</strong> 意図を持ってデザインし、可能な限りデータで検証する。</li>
</ul>

<ul>
    <li><strong>明確さを提供する:</strong> 同じ問題には同じ解決策を適用する。慣れ親しまれたパターンを変更するのは、それが明確な改善である場合のみとする。(一貫性は独自性に勝り、整合性は一貫性に勝る。)</li>
</ul>

<ul>
    <li><strong>反復的にデザインする:</strong> 最初の解決策が完璧であることはない。フィードバックから学び、改善を重ねる。</li>
</ul>

<ul>
    <li><strong>協働を大切にする:</strong> ソフトウェア開発はチームスポーツである。デザインは、エンジニアリングとプロダクトとの協働があってこそ実現できる。</li>
</ul>

<p>
原則について合意が得られた後、主要な画面 ― テーブルビュー、トラッキングページ、コンタクトプロフィール ― のビジュアルディレクションを反復的に検討しました。何度かのフィードバックを経て、チームは最終的なスタイルにたどり着きました。
</p>

<Images name={props.images.md["visual-direction-01"]} captions="新しいテーブルデザインの初期バージョン。"/>

<Images name={props.images.md["visual-direction-02"]} captions="Web トラッキングページのバリエーション。"/>

<Images name={props.images.md["visual-direction-final-01"]} captions="最終的に採用されたデザインスタイル。" className="mt-0"/>

</section>

<section id="design-file-to-a-system-in-code">

#### デザインからコードへ
## デザインツールからコードベースへとシステムを移行させていくほど、エンジニアリングにとっての価値も大きくなっていきました。

<p>
このシステムを構築するには数年を要し、ツールとアーキテクチャの面で大きく 3 つの転換期がありました。
</p>

<p>
<strong>Sketch の時代</strong><br />
最初のバージョンのデザインシステムは Sketch で構築しました。それぞれの UI 要素を個別のライブラリとして管理し、マスターファイルをエントリーポイントとする構成です。システムは 4 つの主要なコンポーネントカテゴリーをカバーしていました。
</p>

<Images name={props.images.md["Components-1"]} captions="初期の Sketch 版デザインシステム。"/>

<p>
本番運用を約 2 年続けた頃、無視できない 2 つの問題が浮き彫りになってきました。1 つは、コンポーネントの更新コストが高かったことです。変更のたびにファイル間を行き来する必要があり、コンポーネントの状態が古いままになりがちでした。もう 1 つは、Sketch のプロトタイピング機能が限定的になりすぎたことです。静的な画面を超える表現には、別のツールを併用せざるを得ませんでした。
</p>

<p>
デザインツールに加えて、開発者にもシステムを使ってもらえるよう、独自のドキュメントサイトも構築しました。しかし、それだけの工数をかけたにもかかわらず、開発者側での定着は進みませんでした。デザインは Sketch 内で完結し、コードはコードベース内で完結しており、ドキュメントサイトはその中間に位置するものの、どちらのワークフローの中心にもなれませんでした。
</p>

</section>

<p>
<strong>Figma への移行(2023)</strong><br />
2023 年初頭、システム全体を Figma に移行しました。移行の決断は、Sketch における 2 つの具体的な問題によるものです。1 つは、パフォーマンスがファイル間の切り替えを日常的なコストにするほど低下していたこと。もう 1 つは、当時すでに不可欠となっていた変数(Variables)など、Sketch には備わっていない機能があったことです。さらに Figma では、システム全体を 1 つのファイルにまとめても、パフォーマンス上の問題が発生しませんでした。
</p>

<Images name={props.images.md["master-file-2"]} className="mt-5" captions="Sketch とは異なり、すべてのコンポーネントを 1 つのファイルにまとめてもパフォーマンスに目立った影響はありませんでした。"/>

<p>
<strong>コードベース上のデザイントークン</strong><br />
今回の移行は、より難しい次の課題に取り組むための前提条件でした。その課題とは、デザインシステムの基盤をコードベースそのものに統合することです。
</p>

<p>
色、タイプスケール、スペーシング、ボーダーラジアス、シャドウなどのデザイントークンを唯一の信頼できる情報源(Source of Truth)として定義し、フロントエンドのコードベースにコードレベルのプリミティブとして実装しました。これによって、システムの基盤はデザインファイル上だけでなく、コードの中にも存在するようになりました。
</p>

<Images 
    containerType="full" 
    className="bg-gray-0" 
    containerWidth="Xl"
    name={props.images.xl["token_architecture_diagram-jp"]}
    captions="トークンは TypeScript で一度だけ定義し、React 向けの JS オブジェクトと、スタイルシート向けの SCSS 変数の両方として書き出されます。"
/>

<p>
<strong>ハードコードされた値を Lint で防ぐ</strong><br />
コードベースにトークンを導入しても、エンジニアが CSS ファイルに `#3B82F6` のような値を直接書ける状態では、その価値はほとんどありません。そこで私は、コードベース内の色、スペーシング、タイポグラフィなど、ハードコードされた値をすべて検出する Lint ルールを自ら作成しました。このルールにより、トークンセットに由来しない値が含まれている限り、マージが通らないようになっています。
</p>

<p>
これによって、デザインレビューの仕事から 1 つのカテゴリーが丸ごとなくなりました。ハードコードされた値を目視で見つける作業は、ビルドが先に検出してくれるため、私の QA プロセスから完全に外れています。
</p>

```javascript
'scale-unlimited/declaration-strict-value': [
    [
        'color',
        'background',
        'background-color',
        'border-color',
        '/^(padding|margin)/',
        'border-radius',
        'font-size',
        'font-weight',
    ],
    {
        ignoreValues: [
            'currentColor',
            'inherit',
            'transparent',
            'auto',
            'none',
            0,
            '/calc/',
            'unset',
        ],
        message:
            'Expected ${types} (expect for padding/margin/border-radius/font-weight) for "${value}" of "${property}"',
    },
],
```
<CodeCaption>システムを強制可能にしているルール。色、スペーシング、タイポグラフィ、ボーダーの値はすべてトークンから取得する必要があり、そうでない場合はビルドが通りません。</CodeCaption>


<p>
<strong>ドキュメント層としての Storybook</strong><br />
以前の独自ドキュメントサイトがうまくいかなかったのは、エンジニアリングのワークフローの外側に置かれていたためでした。そこで私は、コンポーネントのドキュメントを Storybook に移行し、フロントエンドアプリケーションに直接統合しました。これによってエンジニアは、自分の開発環境から離れることなく、コンポーネントを閲覧し、単体でテストし、参照できるようになりました。定着はすぐに進みました。現在では約 10 名のエンジニアが、コードベースを離れることなく、日常のワークフローの一部として Storybook でコンポーネントを閲覧・テスト・参照しています。
</p>

<Images name={props.images.md["live-compoenents-jp"]} captions="コードベースに直接統合されたドキュメントサイトを使うことが、定着を促すうえで欠かせないステップでした。"/>

<p>
もう 1 つのメリットは、後になってようやく明確に見えてきました。Storybook がコードベース内に構造化された機械可読なコンポーネントドキュメントとして存在することで、AI ツールが参照できる基盤にもなったのです。これは、エージェント型デザインへの展開が直接的に依存している、重要なインフラとなっています。
</p>

<section id="design-token-sync">

#### デザイントークン同期
## トークンは Figma とコードベースの間で常に同期され、すべての変更はレビューを経たプルリクエストとして両者の間を行き来するようになりました。

<p>
トークンをコードベースに実装したことで解決したのは、問題の半分にすぎませんでした。同じトークンは、Figma ファイルの中にも変数(Variables)とスタイルとして存在していました。デザイナーとエンジニアがそれぞれ別の側を編集しているにもかかわらず、両者が一致し続けていることを保証する仕組みはありませんでした。そのため、誰かがプロダクト内の誤った色に気づくまで、ズレは見過ごされたままでした。
</p>

<p>
わかりやすい解決策は、Figma からエクスポートしてコードを上書きする一方向のコピーです。しかし、既存のツールはどれもこの方式であり、私たちのニーズには合いませんでした。どちらの側も変更され得る以上、一方向のコピーでは、参照されない側の変更が消えてしまうからです。そこで私は、異なるアプローチでシステムを設計・構築しました。コードを基準とし、ツールはどちらかを上書きするのではなく、両者の差分を可視化します。
</p>

<p>
<strong>仕組み</strong><br />
両者のインターフェースとなるのは、git にコミットされた 2 つのスナップショットファイルです。コードベースは、自身のトークンをあらかじめ Figma の形式に変換した状態で公開しており、そのスナップショットが古くなるとテストが失敗します。私が開発した Figma プラグインは、ライブのファイルを読み取り、コード側のスナップショットを取得して、何が異なるのかをデザイナーに正確に表示します。エンジニアがトークンを変更した場合、そのトークンが Figma に対応づけられなくなっているとプルリクエストは失敗し、デザイナーが自動的にレビュアーとして追加されます。デザイナーがプラグインから変更をプッシュした場合は、インポートジョブがそれを通常のプルリクエストに変換し、人間がレビューします。Figma が自動的に書き換えられることはなく、どちらの方向でも、変更が気づかれないまま反映されることはありません。
</p>

<Images 
    withZoom
    containerType="full" 
    className="bg-gray-0" 
    containerWidth="Xl"
    name={props.images.xl["figure-1-architecture-ja"]}
    captions="全体の構成。Figma ファイル、プラグインのリポジトリ、そしてコードベース。各矢印は、データを受け渡す仕組みと、そこで使われる認証情報を示しています。"
/>

<p>
<strong>初回の実行で見つかったもの</strong><br />
完成した比較機能を実際のファイルに対して初めて実行したところ、何か月も気づかれていなかった 4 件の本物の不整合が見つかりました。16 個の変数がローカルのプリミティブではなく公開ライブラリのコピーを参照していたこと。カラーランプが 1 段階ずれていたこと。グラデーションの角度が誤っていたこと。そして、コード側に対応するもののないエフェクトスタイルが 1 つ残っていたことです。名前の監査も値の抜き取りチェックも行われていましたが、これらは既存のどのチェックにも見えないものでした。4 件はすべて修正され、現在このファイルはズレゼロ、すべてのトークンが一致した状態です。
</p>

<Images 
    withZoom
    containerType="full" 
    className="bg-gray-0" 
    containerWidth="Xl"
    name={props.images.xl["figure-2-figma-to-code-ja"]}
    captions="Figma からコードへのフロー。プラグインがコードを直接編集することはありません。読み取った内容を記録し、Jenkins のジョブがそれをプルリクエストに変換して、人間がレビューします。"
/>

<Images 
    withZoom
    containerType="full" 
    className="bg-gray-0" 
    containerWidth="Xl"
    name={props.images.xl["figure-3-code-to-figma-ja"]}
    captions="コードから Figma へのフロー。プルリクエスト上のチェックが、すべてのトークンを Figma で表現できることを検証します。マージ後、プラグインがデザイナーに、手動で反映すべき変更内容を正確に伝えます。"
/>

</section>

<section id="future_improvements">

#### 今後の改善点
## トークンの基盤がデザインとコードの間で共有された今、次のステップはコンポーネントを一本化し、このシステムを AI ツールが生成の対象とする基盤として活用していくことです。

<p>
トークン層、Lint ルール、そして同期システムによって、デザインとコードの間にあるギャップは基盤レベルで埋まりました。色、スペーシング、タイポグラフィなどの判断は必ずシステムに由来する値から行われ、両者が気づかれないままずれていくこともなくなりました。しかし、その基盤の上、つまりコンポーネントのレベルでは、デザインとコードは依然としてほぼ別々の環境に存在しています。
</p>

<p>
次のステップは、同じ考え方をコンポーネントにも適用することです。デザインファイル内のコンポーネントと本番コードのコンポーネントを同期させ、デザイナーとエンジニアが、並行して別々に実装されたものではなく、同じコンポーネントを参照できるようにします。現在、リードエンジニアと共に、これを最小限の工数と低コストで実現するためのアーキテクチャを設計しています。この統合があってこそ、エージェント型デザインの方向性が単なる理想論ではなく、現実的に成り立つものになります。
</p>

<p>
エージェント型に向けた取り組みは、すでに始まっています。現在多くのチームは、AI を使って単体のモックアップやスキャフォールディングを生成しています。しかし、より本質的な課題はその上流にあります。それは、AI ツールがトークン・コンポーネント・ルールを読み取り、それに準拠した完全なデザインやプロトタイプを生成できるよう、デザインシステム自体を再構築することです。社内では、現在のデザインシステムに基づいて AI が生成した初期のプロトタイプを、PM やエンジニアと共有しています。Storybook は、AI ツールに対して構造化された機械可読なインターフェースを提供するため、この取り組みを可能にする中心的なインフラとなっています。長期的な目標は、デザインからプロトタイプまでのサイクルを大幅に圧縮し、本番に近い品質のデザインを、デザインシステムの実際のコンポーネントとトークンを用いて生成できるようにすることです。
</p>

<p>
まだ実験段階ではありますが、これはシステムの次のメジャーバージョンとして、私が今まさに構築を進めている方向性です。
</p>

</section>

<section id="conclusion">

#### まとめ

<p>
デザインシステムの構築は、プロダクトのリデザインを必要としたスケーラビリティと一貫性の課題に対応するために不可欠でした。最初は Sketch のライブラリとして始まったものが、トークンによって制約された、コードベースに統合され、Storybook でドキュメント化されたシステムへと成長しました。その基盤は現在、Figma とコードの間で同期された状態に保たれており、その上で AI による生成支援へと進化を続けています。それぞれのアーキテクチャ的なステップは、現実の課題を解決するとともに、次のステップに進むための条件を整えてきました。
</p>

<ul>
    <li><strong>デザイナーとして:</strong> このシステムをリードしているということは、モックアップをゼロから描くのではなく、実績のあるコンポーネントから組み立てられるということです。また、トークンやコードのレベルで仕事をすることで、デザインを行う際にエンジニアリング的な視点を持つことが自然になりました。</li>
    <li><strong>エンジニアにとって:</strong> 明確なコンポーネントドキュメント、再利用可能なパターン、トークンシステムを強制する Lint ルールがあることで、機能を出荷するたびにスタイルを延々と作り直すのではなく、ロジック・テスト・パフォーマンスに集中できるようになりました。</li>
    <li><strong>ビジネスにとって:</strong> 最も強力な証拠は、その持続性です。このシステムはツールの全面的な移行を乗り越えて存続し、約 10 名のエンジニアが日々使い続けており、今日に至るまで、出荷されるすべての機能の唯一の信頼できる情報源であり続けています。</li>
</ul>

<ImageSlider 
    beforeKey={props.images.md["before-profile-before"]} 
    afterKey={props.images.md["before-profile-after-jp"]} 
    captions="コンタクトプロフィールページの Before / After。"
/>

<ImageSlider 
    beforeKey={props.images.md["before-table-before"]} 
    afterKey={props.images.md["before-table-after-jp"]} 
    captions="コンタクト一覧ページの Before / After。"
/>

<ImageSlider 
    beforeKey={props.images.md["before-dashbaord-before"]} 
    afterKey={props.images.md["before-dashbaord-after-jp"]} 
    captions="ダッシュボードページの Before / After。"
/>
</section>
</div>
